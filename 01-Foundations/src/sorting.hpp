#pragma once

#include <vector>



void insertion_sort(std::vector<int> &A);//done
void selection_sort(std::vector<int> &A);//done
void merge_sort(std::vector<int> &A, int begin_index, int end_index);//done
void merge(std::vector<int> &A, int begin_index, int mid_index, int end_index);//done
void bubble_sort(std::vector<int> &A);//done
void bubble_sort_v2(std::vector<int> &A);
void heap_sort(std::vector<int> &A);//done


int linear_search(std::vector<int>a, int key);//done
int callbinarySearch(std::vector<int> &A, int key, int begin_index, int end_index);//done
int binary_search(std::vector<int>a, int key);//done
